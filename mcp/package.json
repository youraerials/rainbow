{
  "name": "rainbow-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP servers for Rainbow service coordination",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/mcp-gateway",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
